package com.example.fitpal.ui.theme

import androidx.compose.ui.graphics.Color

// Canvas & Surfaces - Precision Dark Obsidian & Carbon
val DarkOledCharcoal = Color(0xFF090A0C)       // Primary Canvas / Background (Obsidian carbon, never pure black #000)
val DarkMutedGreenBlack = Color(0xFF111317)    // Primary Surface & Card Background
val DarkElevatedSurface = Color(0xFF181B20)    // Secondary Elevated Surface / Micro-containers
val DarkSurfaceStroke = Color(0x14FFFFFF)      // 1dp subtle border: rgba(255,255,255,0.08)

// Primary Active / Critical Accent - Acid-Lime (#CEFD1A as in prompt and reference image)
val VividElectricLime = Color(0xFFCEFD1A)     // Primary Active Accent (#CEFD1A)
val ElectricLimeGlow = Color(0x33CEFD1A)      // Subtle glow / halo
val MutedForestOlive = Color(0xFF262C20)      // Subtle dark container accent
val DarkForestContainer = Color(0xFF1C2216)   // Low-saturation container

// Functional Accents & Metric Indicators
val WarmBrownBronze = Color(0xFF4A3828)       // Earthy Bronze
val EarthyGold = Color(0xFFE5A93C)            // Amber Gold for carbs / warnings
val CoralOrange = Color(0xFFFF5252)           // Coral Red for limits / over-burn
val VibrantCyan = Color(0xFF2FD6D6)           // Cyan accent for protein
val ActiveBlue = Color(0xFF388BFD)            // Subtle water accent

// Text & Monospace Metric Contrast
val CrispWhite = Color(0xFFF0F3F6)            // Crisp, high-contrast, slightly warm neutral white
val MutedGrey = Color(0xFF717886)             // Secondary text / metrics (#717886)
val DarkSubtleGrey = Color(0xFF484F58)        // Subtle tertiary borders & dividers
