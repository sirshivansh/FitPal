{
  "rules": {
    "users": {
      "$uid": {
        // Only allow authenticated users to read and write their own profile and backup records
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}
